# 檔案檢查器

檔案檢查器透過一次串流讀取來檢查檔案. 它會計算 CRC32, MD5, SHA-1, SHA-256 和 SHA-512 校驗碼, 識別常見檔案簽章, 並報告基本中繼資料.

MD5 和 SHA-1 會標示為舊版演算法. 貼上預期校驗碼即可驗證. 還可以複製單個校驗碼, 或複製並分享完整檢查報告.

外掛程式需要主程式組建版本 5268 或更新版本.

安全與私隱限制:

- 拒絕超過 8 TiB 的檔案.
- 檔案內容僅開啟一次, 並以唯讀模式依序處理.
- 僅將前 64 個位元組作為檔案標頭進行檢查.
- 外掛程式不存取網絡, 也不要求儲存空間權限.
