<div align="center">
  <p>
    <img src="https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/app/src/main/res/mipmap/ic_launcher.png?raw=true" alt="File Inspector" width="128" />
  </p>

  <h1>File Inspector</h1>

  <p>Плагин файлового менеджера AutoJs6: семь контрольных сумм за одно чтение, вставьте опубликованное значение — и целостность проверена мгновенно, а заодно виден настоящий формат файла</p>

  <p>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/actions/workflows/ci.yml"><img alt="CI" src="https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/actions/workflows/ci.yml/badge.svg"/></a>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/releases"><img alt="GitHub release (latest by date)" src="https://img.shields.io/github/v/release/SuperMonster003/AutoJs6-Plugin-File-Inspector?label=Release"/></a>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/issues"><img alt="GitHub closed issues" src="https://img.shields.io/github/issues/SuperMonster003/AutoJs6-Plugin-File-Inspector?color=A24232&label=Issues"/></a>
    <a href="https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/LICENSE"><img alt="GitHub License" src="https://img.shields.io/github/license/SuperMonster003/AutoJs6-Plugin-File-Inspector?color=534BAE&label=License"/></a>
  </p>
</div>

### Языки (Languages)

Этот README доступен на следующих языках:

- [简体中文 [zh-Hans]](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/.readme/README-zh-Hans.md)
- [繁體中文 (香港) [zh-Hant-HK]](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/.readme/README-zh-Hant-HK.md)
- [繁體中文 (台灣) [zh-Hant-TW]](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/.readme/README-zh-Hant-TW.md)
- [English [en]](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/.readme/README-en.md)
- [Français [fr]](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/.readme/README-fr.md)
- [Español [es]](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/.readme/README-es.md)
- [日本語 [ja]](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/.readme/README-ja.md)
- [한국어 [ko]](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/.readme/README-ko.md)
- Русский [ru] # текущий
- [العربية [ar]](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/.readme/README-ar.md)

### О проекте

Инспектор файлов (File Inspector) — плагин-компаньон файлового менеджера AutoJs6. Выберите «Проверить файл» для любого файла — плагин прочитает его ровно один раз и одновременно вычислит контрольные суммы CRC32, MD5, SHA-1, SHA-224, SHA-256, SHA-384 и SHA-512, а также покажет на одном экране настоящую сигнатуру формата, размер и первые байты файла. Всё работает только на чтение: плагин не запрашивает разрешений на хранилище и сеть и никогда не изменяет исходный файл.

На странице отчёта есть поле «Проверка целостности»: вставьте контрольную сумму, опубликованную издателем файла, — плагин сам определит алгоритм и даст однозначный вывод «совпадает / не совпадает», так что сверять длинные шестнадцатеричные строки на глаз больше не нужно. Каждую сумму и снимок заголовка можно скопировать отдельно; перед копированием всего отчёта или системной отправкой выберите Markdown либо JSON.

### Возможности

- Одно чтение — все результаты: файл читается последовательно ровно один раз, и все семь контрольных сумм считаются одновременно; большие файлы не ждут нескольких проходов.
- Вставил — проверил: поддерживаются hex и отпечатки, Base64, SRI и полные строки `md5sum` / `sha256sum`; алгоритм определяется автоматически, а отличающееся имя файла вызывает предупреждение.
- Настоящий формат: распознаёт 26 распространённых сигнатур в ограниченной начальной выборке и по фиксированным смещениям, поясняет назначение ZIP-контейнеров APK/JAR/Office и оценивает текстовый либо двоичный характер по доле печатных символов и энтропии.
- Заголовок как на ладони: первые 64 байт файла показываются в сопоставлении «hex + ASCII», плюс определяются метки порядка байтов (BOM) UTF-8 / UTF-16 / UTF-32.
- Двойная сверка размера: отображаются и заявленный файловым менеджером, и фактически прочитанный размер; если файл изменился во время чтения, проверка сразу завершается ошибкой — так выявляются недокачанные и ещё записываемые файлы.
- Управляемый прогресс: для больших файлов видны прочитанные байты, скорость и оценка оставшегося времени; чтение можно отменить в любой момент, а неудачную проверку — повторить одним касанием.
- Результаты сразу в дело: отдельно копируйте любую сумму или снимок заголовка, а весь экранный результат экспортируйте как Markdown либо JSON в буфер обмена или системную панель, не создавая файл.
- Устаревшие алгоритмы помечены: MD5 и SHA-1 несут значок Legacy — напоминание, что они больше не годятся как доказательство безопасности.

### Как пользоваться

1. Скачайте APK и одноимённый файл проверки `.apk.sha256` из одного официального Release, установите APK и включите его в центре плагинов AutoJs6 (требуется AutoJs6 с кодом версии 5268 или новее).
2. Перед обновлением проверьте скачанный APK уже установленным Инспектором файлов; при первой установке сохраните APK и проверьте его после включения плагина. Вставьте 64-значный SHA-256 из файла проверки и убедитесь в зелёном совпадении; оба файла должны быть получены с одной доверенной HTTPS-страницы Release.
3. Откройте файловый менеджер AutoJs6 и найдите файл, который хотите проверить; подойдёт любой обычный файл.
4. Выберите в меню файла «Проверить файл» — чтение начнётся сразу, с живым прогрессом; по завершении появятся контрольные суммы, сигнатура формата и байты заголовка.
5. Чтобы проверить целостность, вставьте опубликованную контрольную сумму в поле «Проверка целостности» и нажмите «Проверить»: зелёный — совпадает, красный — не совпадает.
6. Кнопка рядом с суммой или под снимком заголовка копирует отдельное значение; выберите Markdown либо JSON для копирования или отправки всего отчёта, затем вернитесь в файловый менеджер.

> Поле принимает чистый hex, префиксы вроде `sha256: <значение>` или `MD5=<значение>`, отпечатки `AB:CD:EF`, CRC32 с `0x`, стандартный Base64, SRI вида `sha256-<base64>` и полные строки coreutils `<hex>  <файл>` или `<hex> *<файл>`. Регистр и пробелы по краям игнорируются; алгоритм выводится при уникальной длине, а отличающееся имя файла вызывает предупреждение.

### Распознаваемые сигнатуры форматов

Контрольные суммы работают для любого читаемого обычного файла; вдобавок текущая версия распознаёт следующие сигнатуры в ограниченной начальной выборке или по фиксированным смещениям:

```text
ZIP, 7z, RAR 4, RAR 5, GZIP, XZ, BZIP2, Zstandard, LZ4, TAR, PDF, PNG, JPEG, GIF87a, GIF89a, WebP, MP4 / ISO-BMFF, EBML / Matroska, ELF, DEX, Java Class, Mach-O, PE, SQLite 3, WOFF, WOFF2
```

Распознавание использует ограниченную выборку и структурные поля: метку TAR по смещению 257, `ftyp` ISO-BMFF по смещению 4 и указатель заголовка PE. Это быстрая подсказка, а не полная проверка формата; доля печатных символов и энтропия тоже являются эвристическими оценками. Файлы без совпавшей сигнатуры показываются как «Неизвестно», их суммы считаются как обычно.

### Вопросы и ответы

#### Когда этот плагин полезен?

Классический сценарий — проверка загрузок: получив установщик, прошивку или документ, вставьте опубликованную издателем контрольную сумму SHA-256 (или иную) — и сразу узнаете, полон ли файл и не подменён ли он. Плагин также удобен, чтобы раскрыть настоящий формат файла с подменённым расширением или быстро получить контрольные суммы любого файла для архива и сравнения.

#### Значит ли «совпадает», что файл безопасен?

Совпадение доказывает лишь то, что файл байт в байт идентичен тому, для которого была опубликована сумма; доверие зависит от источника этой суммы. Отдавайте предпочтение значениям SHA-256 или SHA-512, опубликованным официальным источником по HTTPS. Коллизии MD5 и SHA-1 можно изготовить намеренно, поэтому плагин помечает их как Legacy: не считайте их доказательством безопасности.

#### Почему некоторые файлы не удаётся проверить?

Частые причины: файл превышает лимит 8 TiB; файл был изменён другим приложением во время чтения, и фактический размер разошёлся с заявленным; либо выданное хостом разрешение только на чтение истекло. Сообщение об ошибке называет конкретную причину, а «Повторить» запускает проверку заново.

#### Не медленно ли проверяются большие файлы?

Плагин читает файл последовательно один раз и за этот единственный проход считает все семь контрольных сумм, поэтому время ограничено скоростью чтения накопителя, а не числом алгоритмов. Прогресс отображается постоянно, проверку можно отменить в любой момент.

### Разрешения и безопасность

Плагин не запрашивает разрешений на хранилище и сеть и может добраться только до одного файла, выбранного пользователем, — через временный content-URI только для чтения, выданный хостом; по окончании проверки доступ истекает, другие файлы недостижимы. Запросы из файлового менеджера проверяются по каждому полю — идентификатор действия, версия протокола, форма content-URI, имя файла, MIME-тип, заявленный размер, права только на чтение — а любой запрос с правами на запись или постоянными правами отклоняется сразу. Файл обрабатывается потоком только для чтения; в памяти остаются лишь ограниченные буферы, первые 4096 байт для анализа, первые 64 для показа и четырёхбайтовое окно сигнатуры PE, исходный файл не изменяется.

Чтобы каждая проверка оставалась предсказуемой, плагин соблюдает следующие пределы:

- Один файл — не более 8 TiB; за одно действие обрабатывается ровно один файл.
- Для анализа берутся не более первых 4096 байт и четырёхбайтовое окно сигнатуры PE, а показываемый заголовок остаётся длиной 64 байт; ввод ограничен 512 символами, а не-ASCII разрешены только в имени файла coreutils.
- Если фактически прочитанный размер отличается от заявленного, файл считается изменённым, и проверка завершается ошибкой с пояснением.
- Сравнение контрольных сумм выполняется за постоянное время для значений равной длины; MD5 и SHA-1 годятся лишь для сверки старых данных и не должны считаться стойкими к коллизиям.

### Интерфейс плагина

Хост (AutoJs6) обнаруживает и вызывает плагин по следующим идентификаторам; они приведены для разработчиков плагинов и хостов:

```text
service action: org.autojs.plugin.EXPLORER_ACTION
execute action: org.autojs.plugin.EXPLORER_ACTION_EXECUTE
plugin id: file-inspector
engine: explorer-action
variant: default
Explorer action id: inspect-file
MIME type: */*
required host build: 5268
```

Текущая версия добавляет в файловый менеджер хоста действие меню только для чтения над одним файлом; исходный файл не изменяется, каталоги не перечисляются. Если плагин отсутствует или отключён, хост незаметно возвращается к своему поведению по умолчанию.

### Roadmap

Реализованные возможности — это описанное выше и отмеченные пункты Roadmap; будущие работы, включая алгоритмы с дополнительным provider, пакетную проверку и расширения протокола хоста, ведутся там же, а неотмеченные пункты не являются текущими возможностями.

- [ROADMAP.md](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/ROADMAP.md)

### История версий

#### v1.0.1

_2026/08/08_

- `Исправление` Исправлена ошибка, из-за которой хост не мог привязаться к службе плагина после включения в центре плагинов; теперь действие «Проверить файл» доступно сразу после включения
- `Улучшение` Название и описание плагина упрощены, чтобы пользовательская документация читалась естественнее

#### v1.0.0

_2026/08/02_

- `Подсказка` Первый публичный выпуск; требуется AutoJs6 с кодом версии 5268 или новее
- `Функция` Добавлено действие «Проверить файл» (только чтение) в меню файлов файлового менеджера AutoJs6 для обычных файлов любого типа (ID плагина `file-inspector`, ID действия `inspect-file`)
- `Функция` Одно последовательное чтение вычисляет вместе контрольные суммы CRC32, MD5, SHA-1, SHA-256, SHA-512, с живым прогрессом, отменой и повтором
- `Функция` Проверка целостности по вставленной ожидаемой сумме: алгоритм определяется по длине или по префиксам вроде `sha256:`, принимаются запись отпечатков `AB:CD:EF` и префикс `0x` для CRC32, сравнение выполняется за постоянное время для значений равной длины
- `Функция` Отчёт показывает имя файла, MIME-тип, расширение, заявленный и фактический размеры, снимок первых 64 байт в виде hex + ASCII и обнаружение UTF BOM
- `Функция` Распознавание 10 распространённых форматов по сигнатурным байтам в начале файла: ZIP, GZIP, PDF, PNG, JPEG, GIF87a, GIF89a, ELF, DEX, SQLite 3
- `Функция` Каждую сумму можно скопировать отдельно, весь отчёт — скопировать или отправить через системную панель; MD5 и SHA-1 несут значок Legacy
- `Функция` Плагин не запрашивает разрешений на хранилище и сеть и читает файлы только через временный content-URI только для чтения, выданный хостом; лимит 8 TiB на файл
- `Функция` В комплекте тексты интерфейса, инструкции, README и CHANGELOG на 10 языках: упрощённый китайский, традиционный китайский (Гонконг), традиционный китайский (Тайвань), английский, французский, испанский, японский, корейский, русский и арабский
- `Зависимость` Добавлена AndroidX Lifecycle ViewModel 2.9.4

##### Полная история

- [CHANGELOG-ru.md](https://github.com/SuperMonster003/AutoJs6-Plugin-File-Inspector/blob/master/app/src/main/assets/doc/CHANGELOG-ru.md)

### Сборка

```powershell
.\gradlew.bat :app:assembleDebug
```

Release-сборка:

```powershell
.\gradlew.bat :app:appendDigestToReleasedFiles
.\gradlew.bat :app:verifyReleaseChecksums
```

Параметры сборки и подписи задаются в version.properties и sign.properties; текущий минимум — Android 7.0 (SDK 24), целевой SDK — 36.

README, CHANGELOG и встроенные инструкции res/raw*/plugin_instruction.md генерируются скриптом .python/generate_markdown.py из JSON-источников и шаблонов в .readme/ и .changelog/ (10 языков). Чтобы изменить документацию, правьте JSON-источники и запускайте скрипт заново, а не редактируйте сгенерированный Markdown.

### Ссылки

- Документация AutoJs6: https://docs.autojs6.com
- Безопасный обмен файлами в Android: https://developer.android.com/training/secure-file-sharing
